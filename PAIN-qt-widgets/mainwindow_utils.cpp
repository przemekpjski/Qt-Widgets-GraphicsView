#include "mainwindow_utils.h"

#include <QToolButton>
#include <QGridLayout>
#include <QLabel>

namespace mainwindow_utils {
/*
QWidget* createToolButton(const QIcon &buttonIcon, const QString &labelString, QButtonGroup &buttonGroup, const QSize &iconSize)
{
    QToolButton* button = new QToolButton;
    button->setIcon(buttonIcon);
    button->setIconSize(iconSize);
    button->setCheckable(true);
    buttonGroup.addButton(button);  //, id: int(type)

    QGridLayout *layout = new QGridLayout;
    layout->addWidget(button, 0, 0, Qt::AlignHCenter);
    layout->addWidget(new QLabel(labelString), 1, 0, Qt::AlignCenter);

    QWidget *widget = new QWidget;
    widget->setLayout(layout);

    return widget;
}
*/
}
